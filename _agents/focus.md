# Focus

## Immediate Attention

1. Review whether `sections/conclusion.tex` closes the argument set up by the
   introduction and Section 2.
2. Keep future agent summaries in `_agents/`, with `state.md` and `focus.md` as
   short entry points.
3. When collaborators make direct edits outside git-aware changelogs, summarize
   them under `_agents/changelog/auto/`.

## Read First

- `AGENTS.md`
- `_agents/state.md`
- `sections/intro.tex`
- `sections/recap.tex`
- `sections/conclusion.tex`

## Risks

- The conclusion is newly connected to `main.tex`; its framing should be checked
  against the introduction.
- The legacy `agents/` directory is being removed; future agent-facing notes
  should use `_agents/`.

## Needed Feedback

- Does the conclusion synthesize the two arcs without introducing unsupported
  new claims?
